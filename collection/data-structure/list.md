## 数组

### 概念

「数组」是将相同类型的元素存储于 **连续** 内存空间的数据结构。「可变数组」基于数组和扩容机制实现，相比普通数组更加灵活。

### 实现

```python
# 初始化可变数组
array = []

# 索引
array[2]
array[-2]

# 切片
array[:10]
array[1:]
array[::5]

# 尾部添加元素
array.append(2)
```

### 常用算法

- 滑动窗口
- 双指针



